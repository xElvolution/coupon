[package]
name = "coupon_vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
fixed-maturity = []

[dependencies]
solana-program = { workspace = true }
coupon_common = { workspace = true }
