[package]
name = "coupon_market"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []

[dependencies]
solana-program = { workspace = true }
coupon_common = { workspace = true }
coupon_vault = { path = "../coupon_vault", features = ["no-entrypoint"] }
